<?xml version="1.0" encoding="UTF-8"?>
<!--
  Only used by notarized builds — an unsigned build runs without the hardened
  runtime and never reads this file.

  These three are what Chromium's JavaScript engine needs once the hardened
  runtime is switched on. Nothing else is requested: the app has no camera, no
  microphone, and reaches the network only through the ordinary URL loader.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
